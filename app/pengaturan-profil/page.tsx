"use client"

import { useEffect, useState } from "react"
import { useRouter } from "next/navigation"
import { Navbar } from "@/components/piindung/navbar"
import { SimpleFooter } from "@/components/piindung/simple-footer"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import type { AccountProfile } from "@/lib/account-profile"

export default function PengaturanProfilPage() {
  const router = useRouter()
  const [profile, setProfile] = useState<AccountProfile | null>(null)
  const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [phone, setPhone] = useState("")
  const [currentPassword, setCurrentPassword] = useState(""); const [newPassword, setNewPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState("")
  const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [changing, setChanging] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState("")
  useEffect(() => { void fetch("/api/account/profile", { cache: "no-store" }).then(async (response) => { const data = await response.json(); if (!response.ok) throw new Error(data.error); setProfile(data.profile); setName(data.profile.name); setEmail(data.profile.email); setPhone(data.profile.phone) }).catch((cause) => setError(cause instanceof Error ? cause.message : "Profil belum dapat dimuat.")).finally(() => setLoading(false)) }, [])
  async function saveProfile() { setSaving(true); setError(""); setMessage(""); try { const response = await fetch("/api/account/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, email, phone }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); setProfile(data.profile); setMessage("Profil berhasil disimpan.") } catch (cause) { setError(cause instanceof Error ? cause.message : "Profil gagal disimpan.") } finally { setSaving(false) } }
  async function changePassword() { setChanging(true); setError(""); setMessage(""); try { const response = await fetch("/api/account/password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword, newPassword, confirmPassword }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); setCurrentPassword(""); setNewPassword(""); setConfirmPassword(""); setMessage("Password berhasil diubah. Sesi ini telah diakhiri; silakan login kembali."); setTimeout(() => router.push("/login"), 1200) } catch (cause) { setError(cause instanceof Error ? cause.message : "Password gagal diubah.") } finally { setChanging(false) } }
  const initials = profile?.name.split(/\s+/).map((part) => part[0]).join("").slice(0, 2).toUpperCase() ?? "PI"
  return <div className="flex min-h-screen flex-col bg-background"><Navbar /><main className="container mx-auto max-w-3xl flex-1 space-y-6 px-4 py-8"><header><h1 className="text-3xl font-bold">Pengaturan Profil</h1><p className="mt-2 text-muted-foreground">Kelola informasi pribadi dan keamanan akun.</p></header>{error && <div role="alert" className="rounded-xl bg-destructive/10 p-4 text-destructive">{error}</div>}{message && <div role="status" className="rounded-xl bg-emerald-500/10 p-4 text-emerald-700">{message}</div>}{loading ? <div className="rounded-2xl border p-10 text-center text-muted-foreground">Memuat profil...</div> : profile && <><Card><CardHeader><CardTitle>Informasi Profil</CardTitle></CardHeader><CardContent className="space-y-4"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-primary/10 text-xl font-bold">{initials}</div><div><p className="font-medium">Avatar</p><p className="text-sm text-muted-foreground">Upload avatar didefer karena belum tersedia mekanisme terkelola khusus yang aman.</p></div></div><div className="space-y-2"><Label htmlFor="name">Nama Lengkap</Label><Input id="name" value={name} onChange={(event) => setName(event.target.value)} /></div><div className="space-y-2"><Label htmlFor="email">Email</Label><Input id="email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></div><div className="space-y-2"><Label htmlFor="phone">Nomor HP</Label><Input id="phone" type="tel" value={phone} onChange={(event) => setPhone(event.target.value)} /></div><Button onClick={() => void saveProfile()} disabled={saving}>{saving ? "Menyimpan..." : "Simpan Perubahan"}</Button></CardContent></Card><Card><CardHeader><CardTitle>Identitas Anggota</CardTitle></CardHeader><CardContent><p className="font-mono text-lg">{profile.memberId}</p><Button className="mt-4" variant="outline" onClick={() => router.push("/member-area/identitas")}>Buka Kartu Identitas</Button></CardContent></Card><Card><CardHeader><CardTitle>Keamanan Akun</CardTitle><p className="text-sm text-muted-foreground">Setelah password berhasil diubah, cookie sesi saat ini akan dihapus dan Anda harus login kembali. Sesi pada perangkat lain tidak dicabut.</p></CardHeader><CardContent className="space-y-4"><div className="space-y-2"><Label htmlFor="current-password">Password Saat Ini</Label><Input id="current-password" type="password" value={currentPassword} onChange={(event) => setCurrentPassword(event.target.value)} /></div><div className="space-y-2"><Label htmlFor="new-password">Password Baru</Label><Input id="new-password" type="password" value={newPassword} onChange={(event) => setNewPassword(event.target.value)} /></div><div className="space-y-2"><Label htmlFor="confirm-password">Konfirmasi Password Baru</Label><Input id="confirm-password" type="password" value={confirmPassword} onChange={(event) => setConfirmPassword(event.target.value)} /></div><Button onClick={() => void changePassword()} disabled={changing}>{changing ? "Mengubah..." : "Ubah Password"}</Button></CardContent></Card></>}</main><SimpleFooter /></div>
}
