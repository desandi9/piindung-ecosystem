import { redirect } from 'next/navigation';

/** Alias lama sebelum Verifikasi Kordes dipindah ke /gorut-v2/penghimpunan/verifikasi-kordes. */
export default function PenghimpunanKordesRedirectPage() { redirect('/gorut-v2/penghimpunan/verifikasi-kordes'); }
