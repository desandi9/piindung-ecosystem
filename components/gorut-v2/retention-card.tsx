'use client';import type { GorutRetentionPoint } from '@/features/gorut-v2/types';import { StatusBadge } from './status-badge';
export function RetentionCard({data}:{data:GorutRetentionPoint[]}){return <section className="gorut-card gorut-small-panel gorut-panel-enter"><header><div><h2>Retensi Munfiq</h2><strong className="gorut-retention-total">86%</strong><p><StatusBadge value={3.1}/> dibanding bulan lalu</p></div></header><div className="gorut-legend"><span><i className="active"/>Aktif</span><span><i className="new"/>Baru</span><span><i className="inactive"/>Tidak Aktif</span></div><div className="gorut-retention-chart">{data.map((x,i)=><div key={x.id} title={`${x.label}: ${x.active}% aktif`}><span style={{'--retention-delay':`${i*60}ms`} as React.CSSProperties}><i className="active" style={{height:`${x.active}%`}}/><i className="new" style={{height:`${x.new}%`}}/><i className="inactive" style={{height:`${x.inactive}%`}}/></span><small>{x.label}</small></div>)}</div></section>}
